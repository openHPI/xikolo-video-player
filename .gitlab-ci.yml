image: node:11

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - node_modules/

before_script:
  - npm install


test:
  script:
    - npm run test


pages:
  script:
    - npm run build
    - mv www public
  artifacts:
    paths:
      - public
